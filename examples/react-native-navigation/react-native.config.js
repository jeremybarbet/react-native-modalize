module.exports = {
  dependencies: {
    // Looks like auto-linking doesn't work if the project ain't at the root
    'react-native-navigation': {
      platforms: {
        ios: null,
      },
    },
  },
};
