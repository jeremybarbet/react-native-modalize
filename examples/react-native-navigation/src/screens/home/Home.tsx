import React from 'react';
import { Layout, Header, Footer } from 'react-native-modalize/shared';

const HomeScreen = () => (
  <Layout>
    <Header
      subheading="Run with React Native Navigation"
      copy="Go to the modal tab to see all the examples"
    />

    <Footer />
  </Layout>
);

export default HomeScreen;
